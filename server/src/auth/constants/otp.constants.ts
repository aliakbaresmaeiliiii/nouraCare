/** Email OTP validity — must match client EMAIL_OTP_VALIDITY_MS. */
export const EMAIL_OTP_VALIDITY_MS = 15 * 60 * 1000;

/** SMS OTP validity — must match client SMS_OTP_VALIDITY_MS. */
export const SMS_OTP_VALIDITY_MS = 3 * 60 * 1000;
