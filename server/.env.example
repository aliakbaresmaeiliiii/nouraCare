# Required in production (NODE_ENV=production)
JWT_SECRET=generate-a-long-random-secret
DB_PASSWORD=your-database-password

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=dorehealth
DB_NAME=dorehealth

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Comma-separated allowed origins (production). Dev also allows localhost:4200 automatically.
CORS_ORIGINS=capacitor://localhost,https://localhost,http://localhost:4200,https://api.dorehealth.app

# OAuth — verify social tokens server-side (comma-separated if multiple)
GOOGLE_CLIENT_IDS=1088321651982-l914r5o4bj5c73cua6qdcg0ttjnd8hbh.apps.googleusercontent.com
APPLE_CLIENT_IDS=com.tecknnycs.dorehealth,com.tecknnycs.dorehealth.signin

# Public URL for uploaded profile images
BASE_URL=https://api.dorehealth.app

# Display name in outbound emails
APP_NAME=DoreHealth

# Email (OTP / verification) — configure one provider
# MAILGUN_API_KEY=
# MAILGUN_DOMAIN=
# MAIL_HOST=
# MAIL_USERNAME=
# MAIL_PASSWORD=
