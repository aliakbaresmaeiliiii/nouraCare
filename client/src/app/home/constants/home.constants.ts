/** Days of symptom history requested when the home tab loads (was 120). */
export const HOME_SYMPTOMS_HISTORY_DAYS = 30;

/** Minimum ms between automatic home symptom-range refetches (tab re-enter). */
export const HOME_SYMPTOMS_REFETCH_MIN_MS = 60_000;
