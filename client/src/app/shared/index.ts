/**
 * Shared layer public surface.
 * Prefer importing from feature-local paths or these barrels — not from deep service graphs across features.
 */
export { SHARED_STANDALONE_IMPORTS } from './shared-standalone';
